name: Verificar reproducibilidade das regras versus modelos (read-only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verificar:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-leves.txt
      - name: Dependencias
        run: python -m pip install --retries 5 --timeout 120 -r requirements-leves.txt
      - name: Testes das regras e da verificacao
        run: |
          python -m unittest discover -s tests -p "test_regras_preventivas.py" -t tests
          python -m unittest discover -s tests -p "test_comparar_regras_modelos.py" -t tests
      - name: Verificar artefatos congelados (offline)
        run: python src/verificar_artigo_congelado.py
      - name: Recriar chave da conta de servico
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          if [ -z "$GCP_SA_KEY" ]; then echo "Secret GCP_SA_KEY ausente." >&2; exit 1; fi
          printf '%s' "$GCP_SA_KEY" > credenciais_sa.json
      - name: Verificar reproducao sem escrever na planilha
        id: verificacao
        env:
          GOOGLE_APPLICATION_CREDENTIALS: credenciais_sa.json
        run: python src/comparar_regras_modelos.py
      - name: Remover credenciais
        if: always()
        run: rm -f credenciais_sa.json
